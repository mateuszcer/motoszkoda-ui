improve shop signup experience by splitting existing 1 screen into 3:

Step 1: Split into 3 steps using the same stepper component as "Nowe zlecenie" flow.
Step 1 — "Konto" (3 fields):

Email
Hasło
Potwierdź hasło

Step 2 — "Warsztat" (3 fields):
info about shop

Step 3 — "Firma" (3 fields):
billing info

Step 2: Fix the input styling. (this also applies to user signup ofc)
Replace the current underline inputs with filled inputs:
cssinput, textarea {
  width: 100%;
  height: 48px;                    /* textarea: min-height 120px */
  padding: 0 16px;                 /* textarea: 12px 16px */
  background: #EDEEED;             /* --neutral-100 */
  border: 1.5px solid transparent;
  border-radius: 12px;
  font: 400 16px 'DM Sans', sans-serif;
  color: #252825;
}

input:focus, textarea:focus {
  background: #FFFFFF;
  border-color: #6ECFAB;
  outline: none;
  box-shadow: 0 0 0 3px rgba(110, 207, 171, 0.15);
}
```

**Step 3: Layout.**
```
Page background:    #F5F5F3
Card:               white, border-radius 16px, padding 32px, max-width 480px, centered
Stepper:            centered above card, 3 steps: "Konto → Warsztat → Firma"
Title:              "Zarejestruj swój warsztat" — DM Sans 22px weight 600, centered
Subtitle:           "Krok 1 z 3 — Dane logowania" — 14px, #868A86, centered
Fields:             20px gap between each
"Dalej" button:     full-width within card, 48px height, #1B3533 bg, white text, 999px radius
"Wstecz" link:      ghost text link above "Dalej" on steps 2-3, 14px, #656965
Final step button:  "Zarejestruj warsztat" instead of "Dalej"